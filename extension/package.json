{
  "name": "permission-ledger-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev --host 0.0.0.0",
    "build": "vite build && cp manifest.json dist/manifest.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-legacy": "^5.4.0",
    "vite": "^5.4.0",
    "typescript": "^5.8.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
